# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl

# Credentials
*.json

# K8s secrets (apply manually, don't commit real credentials)
**/secret.yaml

# Claude Code
.claude/scheduled_tasks.lock

# OS
.DS_Store
